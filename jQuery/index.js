// jQuery es -> $

// check if jQuery is ready
// then execute the callback function
$(document).ready(function() {
    $("h1").css("color");
});

// an alternative is to put the jQuery at the end but on top of the html
